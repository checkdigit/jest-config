{
  "name": "@checkdigit/jest-config",
  "version": "3.1.0",
  "description": "Check Digit standard Jest configuration",
  "private": false,
  "prettier": "@checkdigit/prettier-config",
  "type": "module",
  "engines": {
    "node": ">=18"
  },
  "peerDependencies": {
    "@types/jest": "29.5.3",
    "dotenv": "16.3.1",
    "jest": "29.6.2",
    "ts-jest": "29.1.1"
  },
  "scripts": {
    "prettier": "prettier --ignore-path .gitignore --list-different .",
    "prettier:fix": "prettier --ignore-path .gitignore --write .",
    "lint": "eslint -f unix --ext .js,.ts src/** *.js",
    "test": "npm run ci:compile && npm run ci:lint && npm run ci:style && npm run ci:test",
    "ci:test": "NODE_OPTIONS=\"--no-warnings --experimental-vm-modules\" jest --coverage=false",
    "ci:coverage": "NODE_OPTIONS=\"--no-warnings --experimental-vm-modules\" jest --coverage=true",
    "ci:compile": "tsc --noEmit",
    "ci:lint": "npm run lint",
    "ci:style": "npm run prettier"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/checkdigit/jest-config.git"
  },
  "keywords": [
    "jest"
  ],
  "author": "Check Digit, LLC",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/checkdigit/jest-config/issues"
  },
  "homepage": "https://github.com/checkdigit/jest-config#readme",
  "devDependencies": {
    "@checkdigit/eslint-config": "^8.2.0",
    "@checkdigit/prettier-config": "^4.0.1",
    "@checkdigit/typescript-config": "4.1.0-PR.41-6096"
  },
  "eslintConfig": {
    "extends": [
      "@checkdigit/eslint-config"
    ]
  },
  "files": [
    "jest-preset.js"
  ],
  "overrides": {
    "typescript": "5.2.1-rc"
  }
}
